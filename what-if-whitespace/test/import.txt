# Tuples

(
  ();
  (a);
  (a,);
  (a,b,c,);
  (a,b,c);
)

==>

SourceFile(InlineBlock(
  Tuple,
  Identifier,
  Tuple(Identifier),
  Tuple(Identifier,Identifier,Identifier),
  Tuple(Identifier,Identifier,Identifier)
))


# MEhhh
((a);)
==>
SourceFile(Identifier)